{
  "name": "level-devil-server",
  "version": "3.0.0",
  "description": "Level Devil multiplayer signaling server",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "build:client": "node scripts/build-client-bundle.js",
    "test:ui": "playwright test",
    "stress:test": "node scripts/tmp-stress-test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2",
    "cors": "^2",
    "dotenv": "^16",
    "express": "^4",
    "jsonwebtoken": "^9",
    "mongoose": "^8",
    "socket.io": "^4"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "socket.io-client": "^4.8.1"
  }
}
